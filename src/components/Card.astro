---
const {title, description, url, image, firstColor, secondColor} = Astro.props
---
<div class=`
  bg-gradient-to-bl ${firstColor} ${secondColor}
  rounded-[48px]
  w-64 sm:w-full h-[500px] sm:h-96 p-6
  flex flex-col justify-between items-center

`>
  <img src={image} alt="Image 1" class="w-40">
  <h3 class="text-3xl font-splineSans-bold">{title}</h3>
  <div class="p-4 rounded-3xl flex-grow self-center">
    <p>{description}</p>
  </div>
</div>