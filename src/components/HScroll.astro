---
import Card from "./Card.astro";
import { getTools } from "../scripts/tools.js"
const myCards = getTools()

const sectionTitle = "Explore"
---
<div class="grid gap-4 grid-flow-col overflow-x-scroll sm:overflow-x-auto sm:grid-cols-3 sm:grid-flow-row">
  {
    myCards.map((card) =>
      <Card 
      title={card.title}
      description={card.description}
      url={card.url}
      image={card.image}
      firstColor={card.firstColor}
      secondColor={card.secondColor}
      />
    )
  }
</div>
